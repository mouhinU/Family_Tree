好的，让我全面审查项目，找出需要处理和优化的地方。


好的，基本规范遵守得不错。让我继续深入检查更多维度。


Now let me check a few more areas to complete the analysis:


经过全面审查，以下是项目各维度的分析和优化建议：

---

## 一、高优先级 — 安全与并发问题

### 1. `RateLimitInterceptor` 线程安全问题
[RateLimitInterceptor.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-web/src/main/java/com/mouhin/family/tree/web/interceptor/RateLimitInterceptor.java#L57-L58) 中 `counter[0]++` 在并发场景下不是原子操作，会导致计数丢失，限流可能失效。

```java
// 当前写法（非线程安全）
int[] counter = rateLimitCache.get(userId, k -> new int[]{0});
counter[0]++;  // 并发时可能丢失更新
```

**建议**：改用 `AtomicInteger` 或 `LongAdder`。

### 2. `FamilyController` 使用 `Map<String, Object>` 接收请求体
[FamilyController.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-web/src/main/java/com/mouhin/family/tree/web/controller/FamilyController.java#L109-L121) 的 `setMemberRole` 和 `updateInfo` 方法使用 `Map` 接收参数，缺少类型安全和参数校验，`Long.valueOf(userIdObj.toString())` 可能抛出 `NumberFormatException`。

**建议**：定义专用的 DTO（如 `MemberRoleDTO`、`FamilyInfoUpdateDTO`），配合 `@Valid` 注解进行参数校验。

### 3. `AuthController` 部分接口缺少登录校验
[AuthController.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-web/src/main/java/com/mouhin/family/tree/web/controller/AuthController.java#L134-L161) 的 `/me`、`/profile`、`/my-node` 接口在 `LoginInterceptor` 的排除路径之外，但方法内部仍手动判断 `userId == null` 返回 401。虽然功能正确，但与全局拦截器逻辑不一致。

**建议**：确认这些路径是否被拦截器覆盖；如果是，可以移除冗余的 null 检查，统一由拦截器处理。

### 4. `AiApplicationService.callLlm()` 手动拼接 JSON
[AiApplicationService.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-application/src/main/java/com/mouhin/family/tree/application/service/AiApplicationService.java#L157-L160) 手动拼接 JSON 字符串，如果 `model` 变量包含特殊字符可能导致 JSON 格式错误。

**建议**：使用 `ObjectMapper` 或 `Map` 构建请求体，避免手动拼接。

---

## 二、中优先级 — 编码规范与架构

### 5. DTO/DO 类使用 `@Data`（违反编码规范）
AGENTS.md 明确规定领域实体使用 `@Getter` + `@Setter`（禁止 `@Data`）。当前有 **约 25 个类**使用了 `@Data`，涵盖：
- **Common 模块 DTO/VO**：[FamilyDTO](file:///Users/mac/CodeDir/Family_Tree/family-tree-common/src/main/java/com/mouhin/family/tree/common/dto/FamilyDTO.java)、[NodeCreateDTO](file:///Users/mac/CodeDir/Family_Tree/family-tree-common/src/main/java/com/mouhin/family/tree/common/dto/NodeCreateDTO.java)、[PageResult](file:///Users/mac/CodeDir/Family_Tree/family-tree-common/src/main/java/com/mouhin/family/tree/common/dto/PageResult.java) 等约 20 个
- **Infrastructure 模块 DO**：[FamilyDO](file:///Users/mac/CodeDir/Family_Tree/family-tree-infrastructure/src/main/java/com/mouhin/family/tree/infrastructure/persistence/entity/FamilyDO.java) 等约 5 个

**建议**：统一替换为 `@Getter` + `@Setter`。DTO/VO 如需要 `toString` 可手动添加。

### 6. 单元测试覆盖率不足
当前仅有 **5 个测试类**，覆盖 application 层的部分服务：

| 已覆盖 | 未覆盖 |
|--------|--------|
| `FamilyNodeApplicationService` | `FamilyApplicationService`（核心家族管理） |
| `FamilyMessageApplicationService` | `FamilyRelationApplicationService` |
| `FamilyTreeApplicationService` | `FamilyGenerationApplicationService` |
| `UserAuthApplicationService` | `FamilyOfferingApplicationService` |
| `ColorLabelEnumTest` | 所有 Domain Service、Infrastructure 层 |

**建议**：优先补充 `FamilyApplicationService`（创建/加入家族、权限校验）和 `FamilyRelationApplicationService`（关系管理）的测试。

### 7. `UserAuthApplicationService` 多次重复查询
[UserAuthApplicationService.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-application/src/main/java/com/mouhin/family/tree/application/service/UserAuthApplicationService.java#L131-L167) 中 `getNickname()`、`getGeneration()`、`getBirthDate()`、`getNodeId()` 四个方法各自独立查询数据库。在 [AuthController.me()](file:///Users/mac/CodeDir/Family_Tree/family-tree-web/src/main/java/com/mouhin/family/tree/web/controller/AuthController.java#L134-L161) 中被连续调用四次，产生 4 次 DB 查询。

**建议**：合并为一个方法返回包含所有字段的对象，一次查询即可。

### 8. `listMyFamilies` N+1 查询
[FamilyApplicationService.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-application/src/main/java/com/mouhin/family/tree/application/service/FamilyApplicationService.java#L378-L389) 中循环内逐个查询家族信息，存在 N+1 问题。

**建议**：先收集所有 `familyId`，批量查询后在内存中组装。

### 9. 魔法值重复定义
`MAX_CONTENT_LENGTH = 500` 在 [FamilyMessage](file:///Users/mac/CodeDir/Family_Tree/family-tree-domain/src/main/java/com/mouhin/family/tree/domain/entity/FamilyMessage.java#L20) 实体和 [FamilyMessageApplicationService](file:///Users/mac/CodeDir/Family_Tree/family-tree-application/src/main/java/com/mouhin/family/tree/application/service/FamilyMessageApplicationService.java#L38) 中各定义了一次。

**建议**：统一使用 `FamilyTreeConsts` 或实体中的常量，应用层不重复定义。

---

## 三、低优先级 — 代码质量与优化

### 10. 应用层重复校验逻辑
[FamilyNodeApplicationService.createNode()](file:///Users/mac/CodeDir/Family_Tree/family-tree-application/src/main/java/com/mouhin/family/tree/application/service/FamilyNodeApplicationService.java#L70-L94) 中手动校验名称长度、性别值、生卒日期等，而 [FamilyNode.validateForCreate()](file:///Users/mac/CodeDir/Family_Tree/family-tree-domain/src/main/java/com/mouhin/family/tree/domain/entity/FamilyNode.java#L48-L59) 已封装了相同校验。应用层应调用领域实体的校验方法，而非重复实现。

### 11. `FamilyTreeDomainService` 职责过重
[FamilyTreeDomainService.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-domain/src/main/java/com/mouhin/family/tree/domain/service/FamilyTreeDomainService.java) 有 428 行，包含树构建、血亲识别、配偶挂载、改嫁优先级、血亲标签计算等多个复杂算法。

**建议**：可考虑拆分为 `TreeBuilderService`（树构建）+ `BloodRelationService`（血亲关系计算）等。

### 12. `application.yml` 日志级别
[application.yml](file:///Users/mac/CodeDir/Family_Tree/family-tree-web/src/main/resources/application.yml#L30) 全局配置 `debug` 级别日志，生产环境应确保由 profile 控制。当前 `application-prod.yml` 未显式覆盖日志级别，可能继承主配置的 `debug`。

**建议**：在 `application-prod.yml` 中显式设置 `logging.level.com.mouhin.family.tree: warn`。

### 13. `FamilyMember.validateRoleChange()` 逻辑不完整
[FamilyMember.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-domain/src/main/java/com/mouhin/family/tree/domain/entity/FamilyMember.java#L61-L71) 中 `validateRoleChange()` 对 OWNER 角色的处理只有一个 `return`，注释说"此处仅做基本校验"，但实际没有任何校验逻辑。

**建议**：补充完整校验或移除该分支，避免误导调用者。

### 14. `FamilyController` 全类名引用
[FamilyController.java](file:///Users/mac/CodeDir/Family_Tree/family-tree-web/src/main/java/com/mouhin/family/tree/web/controller/FamilyController.java#L41) 第 41、57 行使用了 `com.mouhin.family.tree.common.constant.FamilyTreeConsts.SESSION_FAMILY_ID` 全类名引用，而文件头部已经 import 了 `FamilyTreeConsts`。

**建议**：直接使用 `FamilyTreeConsts.SESSION_FAMILY_ID`。

---

## 四、总结

| 分类 | 数量 | 关键项 |
|------|------|--------|
| 安全/并发 | 4 | 限流线程安全、请求体类型安全、JSON 拼接 |
| 编码规范 | 5 | `@Data` 替换、测试覆盖率、N+1 查询 |
| 代码质量 | 5 | 重复校验、领域服务拆分、日志配置 |

**建议优先处理顺序**：线程安全 > DTO 类型安全 > `@Data` 替换 > 补充核心测试 > N+1 优化 > 其余。

需要我帮你逐项修复这些问题吗？